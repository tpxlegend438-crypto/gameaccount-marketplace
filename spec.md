# Specification

## Summary
**Goal:** Rename all visible app branding from the old name to "GAME BAZAR" across the UI.

**Planned changes:**
- Update the HTML `<title>` tag in `index.html` to "GAME BAZAR"
- Update the sticky header logo/title text to "GAME BAZAR"
- Update the hero banner `<h1>` on the Browse page to "GAME BAZAR"
- Replace any remaining visible instances of the old app name ("GameVault", "GameAccount Marketplace", etc.) with "GAME BAZAR"

**User-visible outcome:** The browser tab, header, and hero banner all display "GAME BAZAR" with no trace of the old app name remaining in the UI.
